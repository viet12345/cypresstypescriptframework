{
  "name": "cypresstypescriptframework",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "cypress run",
    "lint": "eslint . --ext .ts,.js",
    "open": "yarn cypress open",
    "reset_frw": "rm -rf node_modules yarn.lock && yarn install",
    "run:test": "yarn cypress run",
    "allure:generate": "allure serve allure-results --clean -o allure-report",
    "allure:open": "npx allure-commandline open allure-report",
    "test:allure": "yarn test && yarn allure:generate && yarn allure:open"
  },
  "devDependencies": {
    "@types/cypress": "^1.1.6",
    "@types/node": "^22.14.1",
    "@typescript-eslint/eslint-plugin": "^8.30.1",
    "@typescript-eslint/parser": "^8.30.1",
    "cypress": "^14.3.1",
    "eslint": "^9.24.0",
    "eslint-plugin-cypress": "^4.2.1",
    "prettier": "^3.5.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
